[package]
name = "example"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
libc = "0.2.139"
log = "0.4.17"
rust-libretro = {  version = "0.3.1", features = [ "log", "unstable-env-commands" ] }